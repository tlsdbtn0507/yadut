# Yadut (야두트) 프로젝트 개발 가이드 및 규칙

이 파일은 야두트 프로젝트의 개발 스타일 가이드, 주요 명칭 및 명령어 규칙을 정의합니다.

## 🚨 개발 프로세스 철칙 (무단 코드 수정 금지)
1. **사전 설명 및 승인 필수:** 유저가 특정 피드백이나 코드 수정 요청을 던졌을 때, **실제 파일 수정 도구(replace_file_content 등)를 사용하거나 빌드 명령을 제안하기 전에 반드시 어떤 파일을 왜, 어떻게 고칠 것인지 한글로 상세한 설명(Step-by-step Plan)을 유저에게 보고해야 합니다.**
2. **무단 수정 및 커밋 금지:** 유저가 "설명 없이 바로 해도 돼"라고 한 경우가 아니라면, 유저의 명시적인 동의 없이 코드를 단독으로 변경하고 검증하거나 푸시/커밋하는 행동은 절대 하지 마십시오.
3. **작업 착수 키워드:** 유저가 계획을 확인하고 `'ㄱㄱ'`, `'ㄱㄱ해'`, `'진행해'`, `'고고'`, `'승인'` 등의 키워드를 입력해 주었을 때만 실제 코드 편집 도구를 호출하여 수정을 시작합니다.

## 🚨 Vercel 배포 및 환경변수 운영 규칙
1. **Preview/테스트 배포 금지:** 이 프로젝트는 개인 프로젝트이므로, 유저가 명시적으로 요청하지 않는 한 Vercel Preview 배포나 별도 테스트 배포를 만들지 마십시오. 개발환경에서 확인 가능한 것은 로컬 개발환경에서 검증합니다.
2. **라이브 환경 보호:** 기존 메인 브랜치 및 라이브 환경을 관리하는 Vercel `project_web` 프로젝트를 임의로 재배포, promote, alias 변경, rollback 하지 마십시오.
3. **Vercel 환경변수 변경 금지:** Vercel Dashboard/CLI/API를 통한 환경변수 추가, 수정, 삭제는 유저에게 위임합니다. AI 에이전트는 필요한 키 이름, 값의 조건, scope만 안내하고 직접 변경하지 않습니다.
4. **Vercel 작업 전 확인:** Vercel 관련 명령을 실행해야 할 가능성이 있으면, 실행 전에 대상 프로젝트명, 브랜치, 환경(Production/Preview/Development), 예상 영향을 한국어로 명확히 보고하고 유저의 명시 승인을 받아야 합니다.

## 1. 프로젝트 주요 개념 및 명칭
- **iOS 앱 이름:** 야두트 (Yadut)
- **대화형 스마트 비서 이름:** 아르커스 (Arcus)
- **디자인 스타일:** 미래지향적인 어두운 테마의 사이버펑크 및 고급 글래스모피즘(Glassmorphic) 네온 UI/UX

## 2. 시스템 아키텍처 규칙
- **클라이언트 (야두트 iOS 앱):** SwiftUI와 Combine 기반. 사진 및 파일 다중 첨부를 지원하며, 씽크패드 서버와 WebSocket을 통해 실시간 통신을 수행합니다.
- **씽크패드 브리지 (ThinkPad Server):** `project_thinkpad` 폴더. FastAPI WebSocket 서버와 텔레그램 봇을 연동합니다. 이미지가 포함된 웹소켓 요청은 바이너리로 디코딩하여 맥북 서버로 바로 라우팅합니다.
- **맥북 아이 (Macbook Server):** `project_mac` 폴더. `/upload` 및 `/sync_calendar` 엔드포인트를 제공하며 macOS 캘린더의 "직장" 캘린더에 연동합니다.

## 3. 문서 및 산출물 규칙
- **구현 계획서:** `implementation_plan.md`는 항상 **한국어**로 작성해야 합니다.

## 🧠 AI-Friendly & Clean Architecture Rules (바이브 코딩 최적화 규격)

마스터와 초고속 바이브 코딩(Vibe Coding)을 진행할 때, AI 에이전트인 너는 스스로의 컨텍스트 윈도우(Context Window)를 보호하고 환각(Hallucination) 및 코드 붕괴를 방지하기 위해 반드시 아래의 **'AI 최적화 클린 코드 지침'**을 준수하여 코드를 작성해야 한다.

### 1. 단일 책임 및 모듈 분리 (Single Responsibility for AI)
* **핀포인트 수정을 위한 분리:** 하나의 함수나 컴포넌트는 오직 **한 가지 역할(Single Concern)**만 수행해야 한다. 코드가 쪼개져 있어야 에러 발생 시 특정 함수(20~30줄 내외)만 AI 컨텍스트에 밀어 넣어 초고속 디버깅이 가능하다.
* **비대한 파일 금지:** `main.py`, `mac_eye.py`, `agent_bridge.py` 등 메인 라우터에 비즈니스 로직을 직접 길게 구현하지 마라. 새로운 기능(예: 파일 생성, 알림, 복합 매크로 등)이 추가될 때는 무조건 `utils/`, `services/`, `modules/` 하위 폴더에 독립된 파일로 분리하고 메인에서는 호출만 하라.
* **함수 길이 제약:** 모든 신규 함수는 최대 30~40줄을 넘지 않는 것을 원칙으로 한다. 이를 초과할 경우 서브 함수로 바인딩하여 분리하라.

### 2. TDD(테스트 주도 개발) 인프라 연동 및 자가 채점
* **테스트 코드 우선 고려:** 새로운 로직이나 엣지 케이스 처리를 구현할 때는, AI 너 스스로가 '자가 채점(Self-Correction)'을 돌릴 수 있도록 `__tests__/` 폴더에 테스트 케이스(예: Vitest, Pytest 등)를 먼저 혹은 동시에 작성하라.
* **회귀 버그 방지:** 기존 코드를 수정할 때 기존 테스트 셋을 깨뜨리지 않는지 항상 먼저 검증하라. 테스트가 통과하는 '녹색 불(Passed)' 기준의 안전한 코드만 최종 제안해야 한다.

### 3. 명세 우선 및 명확한 타입 타이핑 (No Ambiguity)
* **느슨한 타입 금지:** TypeScript(`project_web`, `project_ios`)에서는 `any` 사용을 절대 금지하며, Python에서는 타입 힌팅(Type Hinting)을 필수로 적용하라. 가독성이 좋고 타입이 명확해야 AI가 다음 코드 스텝을 추론할 때 환각을 일으키지 않는다.
* **v1.1 Payload 규격 준수:** `Box 1(ThinkPad)`과 `Box 2(MacBook)` 간의 통신, 혹은 백엔드-프론트엔드 간의 모든 데이터 교환은 사전에 정의된 표준 JSON 포맷 규격을 엄격하게 수호하라. 규격이 모호해지면 즉시 실행을 멈추고 `CLARIFY` 액션을 발동해야 한다.

### 4. 코드 가독성 및 가시성 (Scannability)
* **주석은 AI의 나침반:** Complex한 비즈니스 로직이나 비동기 파이프라인에는 해당 코드의 '의도(Intent)'를 설명하는 간결한 주석을 달아라. 이 주석은 다음 세션에서 다른 AI(혹은 너 자신)가 코드를 읽을 때 컨텍스트를 동기화하는 강력한 힌트가 된다.
* **일관된 네이밍:** 시스템 내부에서 사용하는 핵심 상태나 액션의 결과물은 사전에 합의된 변수명(예: `decision`, `payload` 등)으로 통일하여 코드의 예측 가능성을 극대화하라.

