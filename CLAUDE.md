# Yadut (야두트) 프로젝트 개발 가이드 및 규칙

이 파일은 야두트 프로젝트의 개발 스타일 가이드, 주요 명칭 및 명령어 규칙을 정의합니다.

## 🚨 개발 프로세스 철칙 (무단 코드 수정 금지)
1. **사전 설명 및 승인 필수:** 유저가 특정 피드백이나 코드 수정 요청을 던졌을 때, **실제 파일 수정 도구(replace_file_content 등)를 사용하거나 빌드 명령을 제안하기 전에 반드시 어떤 파일을 왜, 어떻게 고칠 것인지 한글로 상세한 설명(Step-by-step Plan)을 유저에게 보고해야 합니다.**
2. **무단 수정 및 커밋 금지:** 유저가 "설명 없이 바로 해도 돼"라고 한 경우가 아니라면, 유저의 명시적인 동의 없이 코드를 단독으로 변경하고 검증하거나 푸시/커밋하는 행동은 절대 하지 마십시오.
3. **작업 착수 키워드:** 유저가 계획을 확인하고 `'ㄱㄱ'`, `'ㄱㄱ해'`, `'진행해'`, `'고고'`, `'승인'` 등의 키워드를 입력해 주었을 때만 실제 코드 편집 도구를 호출하여 수정을 시작합니다.

## 1. 프로젝트 주요 개념 및 명칭
- **iOS 앱 이름:** 야두트 (Yadut)
- **대화형 스마트 비서 이름:** 아르커스 (Arcus)
- **디자인 스타일:** 미래지향적인 어두운 테마의 사이버펑크 및 고급 글래스모피즘(Glassmorphic) 네온 UI/UX

## 2. 시스템 아키텍처 규칙
- **클라이언트 (야두트 iOS 앱):** SwiftUI와 Combine 기반. 사진 및 파일 다중 첨부를 지원하며, 씽크패드 서버와 WebSocket을 통해 실시간 통신을 수행합니다.
- **씽크패드 브리지 (ThinkPad Server):** `project_thinkpad` 폴더. FastAPI WebSocket 서버와 텔레그램 봇을 연동합니다. 이미지가 포함된 웹소켓 요청은 바이너리로 디코딩하여 맥북 서버로 바로 라우팅합니다.
- **맥북 아이 (Macbook Server):** `project_mac` 폴더. `/upload` 및 `/sync_calendar` 엔드포인트를 제공하며 macOS 캘린더의 "직장" 캘린더에 연동합니다.

## 3. 문서 및 산출물 규칙
- **구현 계획서:** `implementation_plan.md`는 항상 **한국어**로 작성해야 합니다.

